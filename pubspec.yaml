name: reactive_variables
description: This package offers a simple way to create reactive variables, ensuring your UI always stays in sync.
version: 1.0.0
repository: https://github.com/dronn1k-org/reactive_variables
issue_tracker: https://github.com/dronn1k-org/reactive_variables/issues

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
